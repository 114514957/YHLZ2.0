# YHLZ2.0 冻结基线记录

> 记录类型：S0 基线记录  
> 记录时间：2026-08-28 23:19（Asia/Shanghai）  
> 目标目录：`C:\Users\ACE_WAN——PROJECT\YHLZ`  
> 记录用途：为架构冻结、回归比较和回滚提供只读参照。

## 1. 仓库身份

| 项目 | 值 |
|---|---|
| 远程仓库 | `https://github.com/114514957/YHLZ2.0.git` |
| 本地分支 | `main` |
| 上游关系 | `main...origin/main` |
| 基线提交 | `ecee016f8fa0c4be7bf8fdd53ff7113778a3e7d9` |
| 基线提交时间 | `2026-08-12T18:28:15+08:00` |
| 基线提交说明 | 抢救远程文件: README(旧版项目介绍) + .env.example(模板) |
| HEAD 中的文件数 | 1335 |
| Git 子模块 | 无 `.gitmodules`，无子模块 |

仓库由 `git clone --recursive` 获取。Git 对象校验已通过：无 dangling/unreachable 对象、无 garbage 对象。

## 2. 工作树状态

基线代码相对 `HEAD` 没有修改。工作树中的未跟踪文件均为本轮之前已明确创建的规划文档：

```text
docs/YHLZ2.0_架构收敛与冻结_作业指导书_初稿.md
docs/YHLZ2.0_架构收敛与全双工_工程计划书_初稿.md
```

本记录及后续冻结文档属于同一类“规划工件”，不会被视为业务代码变更。未得到明确授权前，不提交、不推送、不删除这些文件。

## 3. 工具和运行环境

| 项目 | 观测值 | 结论 |
|---|---|---|
| Git | `2.55.0.windows.5` | 可用 |
| PowerShell | `7.6.4` | 可用 |
| Python | `python`、`py` 均不在 PATH | 运行验证阻塞 |
| 操作系统查询 | `Get-CimInstance` 被拒绝 | 需在目标运行机补录 |
| 依赖锁定 | 仅有 `requirements.txt`，多为范围版本 | 不可复现风险 |
| 密钥 | 未读取或生成 `.env` | 符合安全要求 |

当前不能执行 Python 导入、FastAPI 启动、单元测试或真实音频测试。因此本记录中的结论分为“静态确认”和“待运行确认”，不能把静态通过当成运行通过。

## 4. 关键文件指纹

以下 SHA-256 用于后续确认关键代码是否被意外改变：

| 文件 | 行数 | SHA-256 |
|---|---:|---|
| `backend/main.py` | 3855 | `52E1C818A4AEFBA4D694BAC1FDA87EF203E10E795271DDD242E2214E7103E2EA` |
| `backend/conversation_manager.py` | 377 | `C31001CE05093EC66382AC3ECEAA2E17E3B9BE24D38BA48F854F83E72BA40267` |
| `backend/conversation_controller.py` | 477 | `6D7270FFAB1B46193452F9A35C62F94FAAB933E92DCD8756327C829DE39BF194` |
| `backend/audio_buffer.py` | 309 | `7CDDE487EB30BC718ACD5C174EDE6E3823CEC586B3EBEDD12E9CD02231179BB4` |
| `backend/vad_engine.py` | 297 | `352B4FFFAA89C83943055EA06E289E00A9794666E050862308929F16DB77923B` |
| `backend/llm_engine.py` | 422 | `6D782C0FABBF87D2495C7F109A3961BFD9EFC15F9D4C684E7494F9B8C3B1EF5C` |
| `backend/tts_engine.py` | 68 | `9643255D462CEEAFE1AC66989B1F169CC59D43C63285FA2238152EEDAB8C6C97` |
| `requirements.txt` | 62 | `68512D41D0D5EA569908C2C49E91C7BD79AC58FE5DF08579094B9AE2592B82E1` |
| `.env.example` | 27 | `9E4758E51180BB0DE2B4C25A84D636A2AF84BC81DA6411056CF95D59B8B131C4` |

## 5. 静态确认的关键事实

1. `backend/main.py:3834` 的 `/ws` 入口调用 `conversation_manager.handle_ws(websocket)`；当前 `backend/conversation_manager.py` 未发现该方法定义。
2. `webui_server.py:1266` 和 `:1277` 的工具代理请求 `/tools`、`/tools/call`；后端已发现的工具路由是 `/agent/tools` 及其子路径。
3. `backend/main.py:2546`、`:2550` 的旧记忆删除/更新接口直接返回成功结果，没有执行持久化操作。
4. `backend/agent/memory/__init__.py` 明确记录旧 `context_manager` 记忆方法为 stub，并保留旧数据库迁移问题说明。
5. `/ws/avatar`、`/ws/stream`、`/ws` 三个 WebSocket 入口与独立语音 Demo 并存，尚未确认是否共享同一个会话所有者。
6. 主服务包含大量声音、感知、理解、视觉记忆、人格和行动路由；这些路由不应在未分类前全部进入核心 Golden Path。

## 6. S0 门禁结果

| 门禁 | 状态 | 说明 |
|---|---|---|
| S0-A 仓库可定位、提交可复现 | 通过 | 远程、分支、提交和文件数已记录 |
| S0-B Git 完整性 | 通过 | `git fsck --full` 无异常 |
| S0-C 运行环境可复现 | 阻塞 | 当前没有 Python 解释器，依赖未锁定 |
| S0-D 入口清单 | 进行中 | 已完成静态初稿，待运行时确认 |
| S0-E 业务行为基线 | 未开始 | 需先准备 Python/依赖和测试设备 |

**当前冻结结论：S0 只能部分通过，禁止进入需要运行代码的迁移阶段。** 可以继续做协议、ADR、静态清单和测试设计。

## 7. 补录要求

在目标运行环境准备好后，必须补录：

- Python 精确版本、虚拟环境路径和 `pip freeze`；
- CUDA、音频输入/输出设备和 FFmpeg 版本；
- `backend/conversation_controller.py` 完整指纹；
- 健康检查、文本 Golden Path、WebSocket 握手和真实 TTS 的结果；
- 连续运行 30 分钟的任务数、队列深度、显存和线程变化。
