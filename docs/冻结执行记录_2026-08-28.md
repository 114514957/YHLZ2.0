# YHLZ2.0 冻结执行记录（2026-08-28）

> 执行批次：FZ-2026-08-28-01  
> 依据：`YHLZ2.0_架构收敛与冻结_作业指导书_初稿.md`  
> 仓库基线：`ecee016f8fa0c4be7bf8fdd53ff7113778a3e7d9`  
> 执行范围：S0 基线、S0 静态盘点、S1 契约提案。  
> 业务代码变更：无。

## 1. 已执行作业

| 作业 | 结果 | 证据 |
|---|---|---|
| 校验远程、分支和 HEAD | 完成 | `origin`、`main`、基线提交已记录 |
| 校验 Git 完整性 | 完成 | `git fsck --full` 无异常 |
| 记录关键文件指纹 | 完成 | 基线记录 + 勘误单 |
| 盘点 HTTP/SSE/WebSocket 入口 | 完成 | `入口与功能清单_初稿.md` |
| 建立风险台账 | 完成 | `冻结前风险台账_初稿.md` |
| 形成 Session Kernel 协议 | Proposed | `Session-Kernel-Protocol-v1_初稿.md` |
| 形成首个 ADR | Proposed | `ADR-001-Session-Kernel边界与双工模式.md` |
| 运行 Python/服务/测试 | 未执行 | 当前环境无 `python`/`py` 命令 |

## 2. 静态门禁结论

### 已通过

- 目标仓库可定位，远程和提交可复现；
- HEAD 包含 1335 个文件，仓库无子模块；
- Git 对象校验通过；
- 关键业务文件指纹与采集值一致；
- `git diff` 中没有业务代码变更。

### 未通过或待确认

- 运行环境不可复现：没有 Python 解释器，依赖也没有锁文件；
- `/ws` 入口调用未发现的 `handle_ws` 方法；
- WebUI 工具代理与 `/agent/tools` 路由不一致；
- 旧记忆更新/删除为空实现，且存在多套记忆存储；
- VAD、turn 所有权、取消传播和真实全双工能力尚未通过运行测试；
- 安全暴露、插件加载和长时间资源稳定性尚未验证。

## 3. 当前决策

状态：**冻结准备阶段，尚未进入代码迁移。**

允许继续：协议评审、ADR、静态架构图、测试用例设计、环境准备和风险处置设计。  
暂不允许：创建 Kernel 实现、改变路由、迁移记忆、删除旧入口或新增 Provider。

## 4. 下一批次准入条件

下一批次必须先完成：

1. 准备 Python 虚拟环境并锁定依赖；
2. 对 `/ws` 和工具路由作“修复 / 适配 / 废弃”决定；
3. 明确 VAD、输入提交、播放停止和 turn 提交的唯一所有者；
4. 明确旧记忆与 Agent Memory 的迁移和删除策略；
5. 建立文本 Golden Path 和最小真实语音测试；
6. 评审并签署 ADR-001，随后补 ADR-002 至 ADR-006。

## 5. 变更纪律

本记录只追加，不覆盖历史结论。任何门禁状态变化必须附命令、测试结果、操作者和时间；任何公共协议变化必须更新协议版本并关联 ADR。
