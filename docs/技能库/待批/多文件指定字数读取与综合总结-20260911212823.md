# 技能申请：多文件指定字数读取与综合总结

触发场景：需要从多个指定文件读取固定字数（或指定范围）的内容，并对这些内容进行统一的提炼、总结或分析时。

步骤：
1. 明确需要读取的文件列表和每个文件的读取限制（如字数限制）。
2. 依次执行文件读取操作（如 `file_read`），确保每个文件都按要求读取。
3. 将所有读取到的文本内容汇集到一个工作区。
4. 根据用户需求（如“综合总结”、“对比分析”、“提取关键点”等），对汇集的内容进行深度处理。
5. 按照用户要求的输出格式和风格（如“简洁直接”、“不罗列背景关系”、“遵循特定原则”）生成最终答案。

状态：待老爹批准
