# 技能申请：多文件指定字数读取与总结

触发场景：需要从多个指定文件读取固定字数（或全部）的内容，并进行后续的综合分析或总结时。

步骤：
1. 明确需要读取的文件列表和对应的读取限制（如字数）。
2. 依次调用文件读取工具，传入文件路径和最大字符数参数。
3. （可选）如果读取过程中出现错误或需要补充读取，则进行错误处理和二次调用。
4. 将所有读取到的内容汇集起来，进行下一步的总结、提炼或分析操作。

状态：待老爹批准
